{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.js": {
      "runtime": "@vercel/node@3.0.7"
    }
  },
  "routes": [
    { "src": "/(api|user|book|auth)/(.*)", "dest": "/api/index.js" },
    { "src": "/[^.]*", "dest": "/index.html" }
  ]
}
